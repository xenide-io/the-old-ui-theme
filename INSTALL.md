# Installation

Use the current [installation guide](./docs/installation.md). The published
package is `@xenide-io/the-old-ui-theme`; legacy `the-old-ui` imports and
`ThemeManager` examples are unsupported.
